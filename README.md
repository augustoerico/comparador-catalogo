# Comparador de catálogo

O comparador de catálogo recebe `catalog-<campanha>.csv` e `store-<date>.csv` e devolve `novo.csv` e `saem.csv`.

## Setup
Install
```bash
npm install
```
Compile
```bash
npx tsc
```

## Run
```bash
node dist/index.js
```